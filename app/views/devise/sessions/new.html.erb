<div style="width: max-content" class="border p-5 rounded rounded-lg shadow-lg">
  <h2>Login</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="mb-3">
      <%= f.label :email, "Email", class: "form-label" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      <div id="emailHelp" class="form-text">Nunca compartilhe seu email ou senha com alguém.</div>
    </div>
    <div class="mb-3">
      <%= f.label :password, "Senha", class: "form-label" %><br />
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
    </div>
    <div class="mb-3 form-check">
      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me, class: "form-check-input" %>
          <%= f.label :remember_me, "Lembrar", class: "form-label" %>
        </div>
      <% end %>
    </div>
    <%= f.submit "Log in", class: "btn btn-primary" %>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
